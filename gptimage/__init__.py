"""GPT Image - CLI for image generation via OpenAI GPT Image models.

Uses an aspect-ratio + resolution vocabulary so the same flags work across models
that accept arbitrary dimensions and models limited to three fixed sizes.
"""

__version__ = "1.0.0"
